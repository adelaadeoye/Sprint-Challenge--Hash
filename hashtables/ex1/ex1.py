def get_indices_of_item_weights(weights, length, limit):
    """
    YOUR CODE HERE
    """
    hashTable={}
    for i in range(length):
        hashTable[i]=weights[i]
        
    print(hashTable)


    return None
